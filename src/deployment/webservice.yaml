NAME: api
LAST DEPLOYED: Thu Dec 22 21:49:18 2022
NAMESPACE: default
STATUS: pending-install
REVISION: 1
TEST SUITE: None
HOOKS:
MANIFEST:
---
# Source: webservice/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: api-webservice
  labels:
    helm.sh/chart: webservice-0.1.0
    app.kubernetes.io/name: webservice
    app.kubernetes.io/instance: api
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: webservice
    app.kubernetes.io/instance: api
---
# Source: webservice/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-webservice
  labels:
    helm.sh/chart: webservice-0.1.0
    app.kubernetes.io/name: webservice
    app.kubernetes.io/instance: api
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: webservice
      app.kubernetes.io/instance: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: webservice
        app.kubernetes.io/instance: api
    spec:
      imagePullSecrets: 
        - name: test  
      serviceAccountName: default
      securityContext:
        {}
      containers:
        - name: webservice
          securityContext:
            {}
          image: "docker.io/nginx:latest"
          imagePullPolicy: IfNotPresent
          env:
            []
          ports:
            - name: http
              containerPort: 80
              protocol: TCP     
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {}
---
# Source: webservice/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-webservice
  labels:
    helm.sh/chart: webservice-0.1.0
    app.kubernetes.io/name: webservice
    app.kubernetes.io/instance: api
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  rules:
    - host: chart-example.local
      http:
        paths:         
          - path: /
            pathType: ImplementationSpecific           
            backend:
              service:
                name: api-webservice
                port:
                  number: 80

NOTES:
1. Get the application URL by running these commands:
