apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zapisywarka-rejestracja.fullname" . }}-test-connection"
  labels:
    {{- include "zapisywarka-rejestracja.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zapisywarka-rejestracja.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
