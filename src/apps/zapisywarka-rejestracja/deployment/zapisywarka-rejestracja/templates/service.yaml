apiVersion: v1
kind: Service
metadata:
  name: {{ include "zapisywarka-rejestracja.fullname" . }}
  labels:
    {{- include "zapisywarka-rejestracja.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zapisywarka-rejestracja.selectorLabels" . | nindent 4 }}
