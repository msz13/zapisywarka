import { CatalogItemsSelectPipe } from './catalog-items-select.pipe';

describe('CatalogItemsSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogItemsSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
