import { AppControlErrorDirective } from './app-control-error.directive';

describe('AppControlErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppControlErrorDirective();
    expect(directive).toBeTruthy();
  });
});
